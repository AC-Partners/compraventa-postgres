<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Anuncio - {{ empresa.nombre }} - Pyme Market</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="{{ url_for('static', filename='favicon.png') }}" type="image/png">

    <style>
        /* Estilos específicos para este formulario */
        .form-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            background-color: #fff;
        }
        .form-title {
            color: #333;
            margin-bottom: 30px;
            text-align: center;
        }
        .form-label {
            font-weight: bold;
            color: #555;
        }
        .form-control, .form-select, .form-control-file {
            border-radius: 5px;
            border: 1px solid #ddd;
            padding: 10px;
            margin-bottom: 15px;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            padding: 10px 20px;
            font-size: 1.1em;
            width: 100%;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .form-group {
            margin-bottom: 1rem;
        }
        /* Estilos para mensajes flash */
        .flash-messages {
            list-style: none;
            padding: 0;
            margin: 20px 0;
            text-align: center;
        }
        .flash-messages li {
            padding: 10px 20px;
            margin-bottom: 10px;
            border-radius: 5px;
            font-weight: bold;
        }
        .flash-messages .success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .flash-messages .danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        .flash-messages .warning {
            background-color: #fff3cd;
            color: #856404;
            border: 1px solid #ffeeba;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <a href="{{ url_for('index') }}" class="logo">
                <img src="{{ url_for('static', filename='favicon.png') }}" alt="Pyme Market Logo">
                Pyme Market
            </a>
            <nav>
                <ul>
                    <li><a href="{{ url_for('index') }}">Inicio</a></li>
                    <li><a href="{{ url_for('publicar') }}">Vender Mi Negocio</a></li>
                    <li><a href="{{ url_for('valorar_empresa') }}">Valorar Mi Empresa</a></li>
                    <li><a href="{{ url_for('estudio_ahorros') }}">Estudio de Ahorros</a></li>
                    <li><a href="{{ url_for('contacto') }}">Contacto</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="container">
        <div class="form-container">
            <h2 class="form-title">Editar Anuncio de {{ empresa.nombre }}</h2>

            {% with messages = get_flashed_messages(with_categories=true) %}
                {% if messages %}
                    <ul class="flash-messages">
                        {% for category, message in messages %}
                            <li class="{{ category }}">{{ message }}</li>
                        {% endfor %}
                    </ul>
                {% endif %}
            {% endwith %}

            <form method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="nombre" class="form-label">Nombre de la Empresa o Negocio <span class="text-danger">*</span></label>
                    <input type="text" class="form-control" id="nombre" name="nombre" value="{{ empresa.nombre }}" required>
                </div>
                <div class="mb-3">
                    <label for="descripcion" class="form-label">Descripción Detallada del Negocio <span class="text-danger">*</span></label>
                    <textarea class="form-control" id="descripcion" name="descripcion" rows="5" required>{{ empresa.descripcion }}</textarea>
                </div>
                <div class="mb-3">
                    <label for="tipo_negocio" class="form-label">Tipo de Negocio <span class="text-danger">*</span></label>
                    <input type="text" class="form-control" id="tipo_negocio" name="tipo_negocio" value="{{ empresa.tipo_negocio }}" required>
                </div>

                <div class="mb-3">
                    <label for="actividad" class="form-label">Actividad Principal (Sector General) <span class="text-danger">*</span></label>
                    <select class="form-select" id="actividad" name="actividad" required>
                        <option value="">Selecciona una actividad</option>
                        {% for actividad_key in actividades %}
                            <option value="{{ actividad_key }}" {% if empresa.actividad == actividad_key %}selected{% endif %}>{{ actividad_key }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="mb-3">
                    <label for="sector" class="form-label">Sub-Sector (Específico) <span class="text-danger">*</span></label>
                    <select class="form-select" id="sector" name="sector" required>
                        {% if empresa.sector %}
                            <option value="{{ empresa.sector }}" selected>{{ empresa.sector }}</option>
                        {% endif %}
                    </select>
                </div>

                <div class="mb-3">
                    <label for="pais" class="form-label">País <span class="text-danger">*</span></label>
                    <input type="text" class="form-control" id="pais" name="pais" value="{{ empresa.pais }}" required>
                </div>
                <div class="mb-3">
                    <label for="ubicacion" class="form-label">Ubicación (Provincia en España) <span class="text-danger">*</span></label>
                    <select class="form-select" id="ubicacion" name="ubicacion" required>
                        <option value="">Selecciona una provincia</option>
                        {% for provincia_opt in provincias %}
                            <option value="{{ provincia_opt }}" {% if empresa.ubicacion == provincia_opt %}selected{% endif %}>{{ provincia_opt }}</option>
                        {% endfor %}
                    </select>
                </div>

                <div class="mb-3">
                    <label for="facturacion" class="form-label">Facturación Anual (€)</label>
                    <input type="number" step="0.01" class="form-control" id="facturacion" name="facturacion" value="{{ empresa.facturacion }}">
                </div>
                <div class="mb-3">
                    <label for="numero_empleados" class="form-label">Número de Empleados</label>
                    <input type="number" class="form-control" id="numero_empleados" name="numero_empleados" value="{{ empresa.numero_empleados }}">
                </div>
                <div class="mb-3">
                    <label for="resultado_antes_impuestos" class="form-label">Resultado Antes de Impuestos (€)</label>
                    <input type="number" step="0.01" class="form-control" id="resultado_antes_impuestos" name="resultado_antes_impuestos" value="{{ empresa.resultado_antes_impuestos }}">
                </div>
                <div class="mb-3">
                    <label for="deuda" class="form-label">Deuda Actual (€)</label>
                    <input type="number" step="0.01" class="form-control" id="deuda" name="deuda" value="{{ empresa.deuda }}">
                </div>
                <div class="mb-3">
                    <label for="precio_venta" class="form-label">Precio de Venta Solicitado (€) <span class="text-danger">*</span></label>
                    <input type="number" step="0.01" class="form-control" id="precio_venta" name="precio_venta" value="{{ empresa.precio_venta }}" required>
                </div>

                <div class="mb-3">
                    <label for="local_propiedad" class="form-label">El local es Propiedad</label>
                    <select class="form-select" id="local_propiedad" name="local_propiedad">
                        <option value="No especificado" {% if empresa.local_propiedad == 'No especificado' %}selected{% endif %}>No especificado</option>
                        <option value="Sí" {% if empresa.local_propiedad == 'Sí' %}selected{% endif %}>Sí</option>
                        <option value="No" {% if empresa.local_propiedad == 'No' %}selected{% endif %}>No</option>
                    </select>
                </div>
                
                <button type="submit" class="btn btn-primary">Actualizar Anuncio</button>
            </form>
        </div>
    </main>

    <footer>
        <div class="container">
            <div class="footer-columns">
                <div class="footer-col">
                    <h4>Pyme Market</h4>
                    <ul>
                        <li><a href="{{ url_for('index') }}">Inicio</a></li>
                        <li><a href="{{ url_for('publicar') }}">Vender Mi Negocio</a></li>
                        <li><a href="{{ url_for('valorar_empresa') }}">Valorar Mi Empresa</a></li>
                        <li><a href="{{ url_for('estudio_ahorros') }}">Estudio de Ahorros</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Legal</h4>
                    <ul>
                        <li><a href="{{ url_for('politica_cookies') }}">Política de Cookies</a></li>
                        <li><a href="{{ url_for('nota_legal') }}">Nota Legal</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Contacto</h4>
                    <ul>
                        <li><a href="{{ url_for('contacto') }}">Contáctanos</a></li>
                        </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; {{ moment().format('YYYY') }} Pyme Market. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // JS para cargar dinámicamente los sub-sectores
        $(document).ready(function() {
            var actividades_sectores = JSON.parse('{{ actividades_dict | tojson | safe }}');
            var actividadSelect = $('#actividad');
            var sectorSelect = $('#sector');
            var initialActividad = actividadSelect.val();
            var initialSector = '{{ empresa.sector | default("", true) }}'; // Obtener el sector actual de la empresa

            function updateSectores(selectedActividad) {
                sectorSelect.empty(); // Limpiar opciones anteriores
                sectorSelect.append($('<option>', {
                    value: '',
                    text: 'Selecciona un sub-sector'
                }));
                if (selectedActividad && actividades_sectores[selectedActividad]) {
                    $.each(actividades_sectores[selectedActividad], function(index, sector) {
                        sectorSelect.append($('<option>', {
                            value: sector,
                            text: sector
                        }));
                    });
                }
            }

            // Cargar sectores al cargar la página si ya hay una actividad seleccionada
            if (initialActividad) {
                updateSectores(initialActividad);
                // Seleccionar el sector actual después de cargar las opciones
                if (initialSector) {
                    sectorSelect.val(initialSector);
                }
            }

            // Cambiar sectores cuando cambia la actividad principal
            actividadSelect.change(function() {
                updateSectores($(this).val());
            });
        });
    </script>
</body>
</html>
